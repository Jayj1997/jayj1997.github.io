<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>使用gin作网关, etcd作服务发现实现go-grpc微服务的保姆级教程</title>
    <url>/2021/11/09/gin_etcd_microservices/</url>
    <content><![CDATA[<p>在网上有着数不清的帖子教学如何单独搭建grpc与etcd、如何使用go-grpc-gateway等等教程，如果你在读过这一大堆的文章教程之后，<br>仍然是没有搞懂网关如何接管RESTAPI的请求并转发、服务间如何通信、网关如何鉴权，以及这每一块积木我都知道是做什么的，到底如何<br>拼起来这样的疑惑，可以通过读完这篇文章，快速构建起整个微服务框架。</p>
<h2 id="Before-Started"><a href="#Before-Started" class="headerlink" title="Before Started"></a>Before Started</h2><p>在开始之前，你应该先有golang的基础，如果还不明白golang怎么用，就先不要看这里了。</p>
<p>如果你想直接看demo，或者后面有减少文章字数需要用到源码的地方，<a href="https://github.com/Jayj1997/grpc_etcd_gin">源码，点击获取</a></p>
<h3 id="etcd"><a href="#etcd" class="headerlink" title="etcd"></a>etcd</h3><p>首先,如果你还未搭建起ETCD服务,可以借鉴这份docker-compose，或者百度谷歌上有各种教程。</p>
<p><a href="https://github.com/Jayj1997/grpc_etcd_gin/blob/main/etcd/etcd-compose.yml">etcd docker-compose file</a></p>
<h3 id="protoc"><a href="#protoc" class="headerlink" title="protoc"></a>protoc</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git <span class="built_in">clone</span> https://github.com/golang/protobuf.git &amp;&amp; <span class="built_in">cd</span> protobuf</span><br><span class="line">go install ./proto</span><br><span class="line">go install ./protoc-gen-go</span><br></pre></td></tr></table></figure>

<h2 id="Let’s-Code"><a href="#Let’s-Code" class="headerlink" title="Let’s Code"></a>Let’s Code</h2><p>首先，你需要两个项目，分别是 gateway 和 ping, 分别是网关和其中的微服务，对于多个微服务之间的通信会在最后提到。</p>
<h3 id="ping-项目结构"><a href="#ping-项目结构" class="headerlink" title="ping 项目结构"></a>ping 项目结构</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">- discovery 服务注册</span><br><span class="line">  - instance.go</span><br><span class="line">  - register.go</span><br><span class="line">  - resolver.go</span><br><span class="line">- proto proto文件</span><br><span class="line">  - pb</span><br><span class="line">    - ping.pb.go</span><br><span class="line">    - ping.proto</span><br><span class="line">- service</span><br><span class="line">  - ping_service.go</span><br><span class="line">- main.go main文件</span><br><span class="line">- go.mod</span><br><span class="line">- go.sum</span><br></pre></td></tr></table></figure>

<h4 id="discovery"><a href="#discovery" class="headerlink" title="discovery"></a>discovery</h4><p>discovery文件是用来向ETCD注册服务、keep-alive、获取服务信息等功能的一套示例，对还在学习如何搭建起一套微服务框架的你，最好在之后再去搞懂这个文件的作用，目前先把源码展示出来，将它按照结构复制进去即可,我会在之后的帖子详细讲述这些文件的内容都做了什么</p>
<p><a href="https://github.com/Jayj1997/grpc_etcd_gin/tree/main/gateway/discovery">discovery</a></p>
<p>在后面说到网关项目的时候，会附上一段如果你不想跑封装好的discovery而是使用原生包的源码，可以在gateway参考一下</p>
<h4 id="proto"><a href="#proto" class="headerlink" title="proto"></a>proto</h4><p>按照结构创建文件，并在user.proto内写入如下内容</p>
<figure class="highlight protobuf"><table><tr><td class="code"><pre><span class="line">syntax = <span class="string">&quot;proto3&quot;</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">package</span> ping;</span><br><span class="line"></span><br><span class="line"><span class="keyword">option</span> go_package = <span class="string">&quot;/proto/pb;pb&quot;</span>;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">service</span> <span class="title">Ping</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">rpc</span> Pong (Req) <span class="keyword">returns</span> (Resp) </span>&#123;&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">message</span> <span class="title">Req</span> </span>&#123;</span><br><span class="line">    <span class="built_in">string</span> name = <span class="number">1</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">message</span> <span class="title">Resp</span> </span>&#123;</span><br><span class="line">    <span class="built_in">string</span> msg = <span class="number">1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在项目文件夹内命令行输入命令生成pb文件</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">protoc -I . --go_out=plugins=grpc:. proto/pb/*.proto</span><br></pre></td></tr></table></figure>

<h4 id="service"><a href="#service" class="headerlink" title="service"></a>service</h4><p>找到pingServer的interface, 并在自己的service文件中补齐interface内的方法</p>
<p>ping.pb.go</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">type</span> PingServer <span class="keyword">interface</span> &#123;</span><br><span class="line"> Pong(context.Context, *Req) (*Resp, error)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>写入service/ping_service.go, 并补齐缺失的import、user的model</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> service</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line"> <span class="string">&quot;context&quot;</span></span><br><span class="line"> <span class="string">&quot;ping/proto/pb&quot;</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> PingService <span class="keyword">struct</span> &#123;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">NewPingService</span><span class="params">()</span> *<span class="title">PingService</span></span> &#123;</span><br><span class="line"> <span class="keyword">return</span> &amp;PingService&#123;&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(s *PingService)</span> <span class="title">Pong</span><span class="params">(ctx context.Context, in *pb.Req)</span> <span class="params">(*pb.Resp, error)</span></span> &#123;</span><br><span class="line"></span><br><span class="line"> msg := fmt.Sprintf(<span class="string">&quot;hello %s&quot;</span>, in.Name)</span><br><span class="line"></span><br><span class="line"> <span class="keyword">return</span> &amp;pb.Resp&#123;</span><br><span class="line">  Msg: msg,</span><br><span class="line"> &#125;, <span class="literal">nil</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="main-go"><a href="#main-go" class="headerlink" title="main.go"></a>main.go</h4><p>最后就是main文件</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line"> <span class="string">&quot;fmt&quot;</span></span><br><span class="line"> <span class="string">&quot;net&quot;</span></span><br><span class="line"> <span class="string">&quot;ping/discovery&quot;</span></span><br><span class="line"> <span class="string">&quot;ping/proto/pb&quot;</span></span><br><span class="line"> <span class="string">&quot;ping/service&quot;</span></span><br><span class="line"></span><br><span class="line"> <span class="string">&quot;github.com/sirupsen/logrus&quot;</span></span><br><span class="line"> <span class="string">&quot;google.golang.org/grpc&quot;</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> (</span><br><span class="line">  <span class="comment">// 微服务名称</span></span><br><span class="line"> app         = <span class="string">&quot;ping&quot;</span></span><br><span class="line"> <span class="comment">// 微服务地址</span></span><br><span class="line"> grpcAddress = <span class="string">&quot;127.0.0.1:10004&quot;</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line"> <span class="comment">// etcd 地址</span></span><br><span class="line"> etcdAddress := []<span class="keyword">string</span>&#123;<span class="string">&quot;127.0.0.1:2379&quot;</span>&#125;</span><br><span class="line"></span><br><span class="line"> <span class="comment">// 服务注册</span></span><br><span class="line"> etcdRegister := discovery.NewRegister(etcdAddress, logrus.New())</span><br><span class="line"> <span class="keyword">defer</span> etcdRegister.Stop()</span><br><span class="line"></span><br><span class="line"> node := discovery.Server&#123;</span><br><span class="line">  Name: app,</span><br><span class="line">  Addr: grpcAddress,</span><br><span class="line"> &#125;</span><br><span class="line"></span><br><span class="line"> server := grpc.NewServer()</span><br><span class="line"> <span class="keyword">defer</span> server.Stop()</span><br><span class="line"></span><br><span class="line"> <span class="comment">// 绑定service</span></span><br><span class="line"> pb.RegisterPingServer(server, service.NewPingService())</span><br><span class="line"></span><br><span class="line"> lis, err := net.Listen(<span class="string">&quot;tcp&quot;</span>, grpcAddress)</span><br><span class="line"> <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">  <span class="built_in">panic</span>(err)</span><br><span class="line"> &#125;</span><br><span class="line"> <span class="keyword">defer</span> lis.Close()</span><br><span class="line"></span><br><span class="line"> <span class="keyword">if</span> _, err := etcdRegister.Register(node, <span class="number">10</span>); err != <span class="literal">nil</span> &#123;</span><br><span class="line">  <span class="built_in">panic</span>(fmt.Sprintf(<span class="string">&quot;start server failed, err: %v&quot;</span>, err))</span><br><span class="line"> &#125;</span><br><span class="line"></span><br><span class="line"> logrus.Info(<span class="string">&quot;server started listen on &quot;</span>, grpcAddress)</span><br><span class="line"> <span class="keyword">if</span> err := server.Serve(lis); err != <span class="literal">nil</span> &#123;</span><br><span class="line">  <span class="built_in">panic</span>(err)</span><br><span class="line"> &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>至此，如果你跟着完成了ping服务，可以跑起项目看一下，如果你有etcd的看板或者你使用了我的compose文件，可以看到/ping/127.0.0.1:10004下的服务</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 权重与版本没有声明，weight用于负载均衡</span></span><br><span class="line">&#123;</span><br><span class="line">  <span class="attr">&quot;name&quot;</span>:<span class="string">&quot;ping&quot;</span>,</span><br><span class="line">  <span class="attr">&quot;addr&quot;</span>:<span class="string">&quot;127.0.0.1:10004&quot;</span>,</span><br><span class="line">  <span class="attr">&quot;version&quot;</span>:<span class="string">&quot;&quot;</span>,</span><br><span class="line">  <span class="attr">&quot;weight&quot;</span>:<span class="number">0</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="gateway-项目结构"><a href="#gateway-项目结构" class="headerlink" title="gateway 项目结构"></a>gateway 项目结构</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">- discovery       服务注册 与ping服务内容一致</span><br><span class="line">- middleware</span><br><span class="line">  - jwt.go        jwt鉴权中间件</span><br><span class="line">- proto proto文件  该文件是ping项目内的proto文件</span><br><span class="line">  - pb</span><br><span class="line">    - ping.pb.go</span><br><span class="line">    - ping.proto</span><br><span class="line">- routers</span><br><span class="line">  - ping.go</span><br><span class="line">  - route.go</span><br><span class="line">- utils</span><br><span class="line">  - res           一些gin相关返回值的封装 复制或者自己写就好</span><br><span class="line">  - jwt.go        jwt鉴权</span><br><span class="line">- main.go         main文件</span><br><span class="line">- go.mod</span><br><span class="line">- go.sum</span><br></pre></td></tr></table></figure>

<p>看到gateway的项目结构可能你会有两个小问题：</p>
<ul>
<li>为什么选择在网关处使用gin而不是grpc-gateway<ul>
<li>答：是因为gin更符合我以前做单体页面时候的习惯，你可以像以前一样使用中间件、路由，还有比这个更好的吗？</li>
</ul>
</li>
<li>为什么gateway里还有微服务的protobuf文件，我以后的项目也要这样吗？<ul>
<li>答：当然不是，我在这里只是偷了个懒，实际生产中你应该将所有的微服务模块上传git服务器，通过包的形式引入各个项目，在服务之间使用服务发现通讯也是如此。假如你有一个非常基础的模块user，模块内有user的model、方法，你更应该将user这个模块通过golang包的形式引入其他项目而不是同时维护多个model（不用想也知道维护多个model的后果是什么吧？）</li>
</ul>
</li>
</ul>
<h4 id="discovery文件"><a href="#discovery文件" class="headerlink" title="discovery文件"></a>discovery文件</h4><p>在ping服务有讲，不再重复</p>
<h4 id="proto文件"><a href="#proto文件" class="headerlink" title="proto文件"></a>proto文件</h4><p>引用了ping服务的proto文件</p>
<h4 id=""><a href="#" class="headerlink" title=""></a></h4><p>routes</p>
<p>ping.go</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> routes</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line"> <span class="string">&quot;gateway/proto/pb&quot;</span></span><br><span class="line"> <span class="string">&quot;gateway/utils/res&quot;</span></span><br><span class="line"></span><br><span class="line"> <span class="string">&quot;github.com/gin-gonic/gin&quot;</span></span><br><span class="line"> <span class="string">&quot;github.com/sirupsen/logrus&quot;</span></span><br><span class="line"> <span class="string">&quot;google.golang.org/grpc&quot;</span></span><br><span class="line"> <span class="string">&quot;google.golang.org/grpc/balancer/roundrobin&quot;</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">addPingRoute</span><span class="params">(rg *gin.RouterGroup)</span></span> &#123;</span><br><span class="line"> opts := []grpc.DialOption&#123;</span><br><span class="line">  grpc.WithInsecure(),</span><br><span class="line">  <span class="comment">// 在这里出现了负载均衡，在完成了整个项目之后你可以创建两个ping服务同时注册</span></span><br><span class="line">  <span class="comment">// 同时通过网关请求多次试一试</span></span><br><span class="line">  <span class="comment">// 你会发现 一般情况下，两个ping服务会平分请求，也就达到了负载均衡</span></span><br><span class="line">  <span class="comment">// 具体均衡规则还可以通过配置weight来实现</span></span><br><span class="line">  grpc.WithBalancerName(roundrobin.Name),</span><br><span class="line"> &#125;</span><br><span class="line"></span><br><span class="line"> <span class="comment">// 通过etcd做服务发现 在这里你并不是通过请求ping服务的10004来连接而是通过etcd去寻找ping服务</span></span><br><span class="line"> pingConn, err := grpc.Dial(<span class="string">&quot;etcd:///ping&quot;</span>, opts...)</span><br><span class="line"> <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">  logrus.Errorf(<span class="string">&quot;try connect ping service failed&quot;</span>)</span><br><span class="line"> &#125;</span><br><span class="line"> <span class="comment">// 不要调用pingConn.Close() 因为addRoute方法只是导入了路由与对应的方法</span></span><br><span class="line"> <span class="comment">// 在读取完毕之后会关闭链接 读取完毕之后的请求会无法链接 因为链接已经关闭</span></span><br><span class="line"> <span class="comment">// 如果你一定要严谨的关闭 就把它移出去到公共的地方注册链接 并创建方法在优雅关闭内结束</span></span><br><span class="line"></span><br><span class="line"> pingClient := pb.NewPingClient(pingConn)</span><br><span class="line"></span><br><span class="line"> <span class="comment">// 在这里 分组 路由的方法等 如果用过单体gin的同学是不是很眼熟了</span></span><br><span class="line"> ping := rg.Group(<span class="string">&quot;/ping&quot;</span>)</span><br><span class="line"> &#123;</span><br><span class="line">  ping.GET(<span class="string">&quot;&quot;</span>, <span class="function"><span class="keyword">func</span><span class="params">(ctx *gin.Context)</span></span> &#123;</span><br><span class="line">   name := ctx.Query(<span class="string">&quot;name&quot;</span>)</span><br><span class="line"></span><br><span class="line">   req := &amp;pb.Req&#123;</span><br><span class="line">    Name: name,</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   resp, err := pingClient.Pong(ctx, req)</span><br><span class="line">   <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">    logrus.Errorln(<span class="string">&quot;/v1/ping err, err: &quot;</span>, err)</span><br><span class="line">    res.InternalError(ctx)</span><br><span class="line">    <span class="keyword">return</span></span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// 这里是自己封装的方法</span></span><br><span class="line">   res.Ok(ctx, res.OK, resp.Msg)</span><br><span class="line">  &#125;)</span><br><span class="line"> &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<p>route.go</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> routes</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line"> <span class="string">&quot;gateway/middleware&quot;</span></span><br><span class="line"> <span class="string">&quot;gateway/utils/res&quot;</span></span><br><span class="line"> <span class="string">&quot;net/http&quot;</span></span><br><span class="line"></span><br><span class="line"> <span class="string">&quot;github.com/gin-gonic/gin&quot;</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="comment">// LoadGin 初始化gin 注册路由</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">LoadGin</span><span class="params">()</span> *<span class="title">gin</span>.<span class="title">Engine</span></span> &#123;</span><br><span class="line"></span><br><span class="line"> g := gin.Default()</span><br><span class="line"></span><br><span class="line"> g.Use(middleware.Cors())</span><br><span class="line"></span><br><span class="line"> getRoute(g)</span><br><span class="line"></span><br><span class="line"> g.NoRoute(<span class="function"><span class="keyword">func</span><span class="params">(ctx *gin.Context)</span></span> &#123;</span><br><span class="line">  res.Error(ctx, http.StatusNotFound, res.UrlNotFound)</span><br><span class="line"> &#125;)</span><br><span class="line"></span><br><span class="line"> <span class="keyword">return</span> g</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">getRoute</span><span class="params">(g *gin.Engine)</span></span> &#123;</span><br><span class="line"></span><br><span class="line"> v1 := g.Group(<span class="string">&quot;/v1&quot;</span>)</span><br><span class="line"> addPingRoute(v1)</span><br><span class="line"></span><br><span class="line">  <span class="comment">// ...</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<h4 id="main-go-1"><a href="#main-go-1" class="headerlink" title="main.go"></a>main.go</h4><p>终于，gateway项目也要完成了（jwt鉴权在跑通gateway与ping之后）</p>
<p>main.go</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line"> <span class="string">&quot;gateway/discovery&quot;</span></span><br><span class="line"> <span class="string">&quot;gateway/routes&quot;</span></span><br><span class="line"> <span class="string">&quot;gateway/utils&quot;</span></span><br><span class="line"> <span class="string">&quot;net/http&quot;</span></span><br><span class="line"> <span class="string">&quot;time&quot;</span></span><br><span class="line"></span><br><span class="line"> <span class="string">&quot;github.com/sirupsen/logrus&quot;</span></span><br><span class="line"> <span class="string">&quot;google.golang.org/grpc/resolver&quot;</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line"></span><br><span class="line"> <span class="comment">// etcd注册</span></span><br><span class="line"> r := discovery.NewResolver([]<span class="keyword">string</span>&#123;<span class="string">&quot;localhost:2379&quot;</span>&#125;, logrus.New())</span><br><span class="line"> <span class="keyword">defer</span> r.Close()</span><br><span class="line"> resolver.Register(r)</span><br><span class="line"></span><br><span class="line"> <span class="comment">// 初始化gin 加载路由</span></span><br><span class="line"> g := routes.LoadGin()</span><br><span class="line"></span><br><span class="line"> server := &amp;http.Server&#123;</span><br><span class="line">  Addr:           <span class="string">&quot;:8080&quot;</span>,</span><br><span class="line">  Handler:        g,</span><br><span class="line">  ReadTimeout:    <span class="number">10</span> * time.Second,</span><br><span class="line">  WriteTimeout:   <span class="number">10</span> * time.Second,</span><br><span class="line">  MaxHeaderBytes: <span class="number">1</span> &lt;&lt; <span class="number">20</span>,</span><br><span class="line"> &#125;</span><br><span class="line"></span><br><span class="line"> <span class="keyword">go</span> <span class="function"><span class="keyword">func</span><span class="params">()</span></span> &#123;</span><br><span class="line">  <span class="comment">// 优雅关闭http server 源码内有该方法，你也可以直接抹掉这一行</span></span><br><span class="line">  <span class="comment">// 为什么要优雅关闭server？</span></span><br><span class="line">  <span class="comment">// https://studygolang.com/articles/12600</span></span><br><span class="line">  utils.GracefullyShutdown(server)</span><br><span class="line"> &#125;()</span><br><span class="line"></span><br><span class="line"> logrus.Info(<span class="string">&quot;gateway listen on :8080&quot;</span>)</span><br><span class="line"></span><br><span class="line"> <span class="keyword">if</span> err := server.ListenAndServe(); err != <span class="literal">nil</span> &#123;</span><br><span class="line">  logrus.Fatal(<span class="string">&quot;gateway启动失败, err: &quot;</span>, err)</span><br><span class="line"> &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>现在 跑起gateway与ping服务，用postman等请求工具请求一下localhost:8080/v1/ping?name=jay</p>
<p>或者直接命令行请求</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">curl -v <span class="string">&quot;http:/127.0.0.1:8080/v1/ping?name=jay&quot;</span></span><br></pre></td></tr></table></figure>

<p>（我这里用的wsl2, 还配置了代理，需要请求windows的地址，所以我习惯用工具请求）</p>
<p>如果你得到的是</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line">&#123;<span class="attr">&quot;code&quot;</span>:<span class="number">1001</span>,<span class="attr">&quot;data&quot;</span>:<span class="string">&quot;hello jay&quot;</span>,<span class="attr">&quot;msg&quot;</span>:<span class="string">&quot;成功&quot;</span>&#125;% </span><br></pre></td></tr></table></figure>

<p>恭喜你已经打通整个微服务，如果你已经可以熟练的使用gin或者知道如何使用jwt中间件，到这里你就完成了完整的流程。好像还有一点没提到，就是两个微服务之间如何相互请求，答案就在gateway的routes/ping.go文件里，通过服务发现去请求咯。</p>
<h3 id="使用jwt中间件鉴权"><a href="#使用jwt中间件鉴权" class="headerlink" title="使用jwt中间件鉴权"></a>使用jwt中间件鉴权</h3><p>如果你还不知道如何使用gin的中间件或者就是想看一下，接下来是如何使用jwt。做法是在网关处统一声明需要和不需要鉴权的路由，在通过http请求的时候，会通过gin来验证权限。而在微服务内部，使用服务发现的形式的请求就不需要鉴权服务了。</p>
<h4 id="middleware-jwt-go"><a href="#middleware-jwt-go" class="headerlink" title="middleware/jwt.go"></a>middleware/jwt.go</h4><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> middleware</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line"> <span class="string">&quot;gateway/utils&quot;</span></span><br><span class="line"> <span class="string">&quot;gateway/utils/res&quot;</span></span><br><span class="line"> <span class="string">&quot;time&quot;</span></span><br><span class="line"></span><br><span class="line"> <span class="string">&quot;github.com/gin-gonic/gin&quot;</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="comment">// JWT token的生成是使用用户名、密码等一系列信息生成 在解密的时候可以拿到这些信息</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">JWT</span><span class="params">()</span> <span class="title">gin</span>.<span class="title">HandlerFunc</span></span> &#123;</span><br><span class="line"> <span class="keyword">return</span> <span class="function"><span class="keyword">func</span><span class="params">(ctx *gin.Context)</span></span> &#123;</span><br><span class="line">  <span class="keyword">var</span> claims *utils.Claims</span><br><span class="line">  <span class="keyword">var</span> err error</span><br><span class="line"></span><br><span class="line">  code := res.OK</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 对于我个人喜欢将鉴权的token放到header中的Authorization中</span></span><br><span class="line">  <span class="comment">// 如果你有自己的想法，可以在这里获取你将token放的位置</span></span><br><span class="line">  token := ctx.GetHeader(<span class="string">&quot;Authorization&quot;</span>)</span><br><span class="line">  <span class="keyword">if</span> token == <span class="string">&quot;&quot;</span> &#123;</span><br><span class="line">   res.Unauthorized(ctx, res.TokenInvalid)</span><br><span class="line"></span><br><span class="line">   ctx.Abort()</span><br><span class="line">   <span class="keyword">return</span></span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 验证</span></span><br><span class="line">  claims, err = utils.ParseToken(token)</span><br><span class="line">  <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line"></span><br><span class="line">   res.Unauthorized(ctx, res.TokenInvalid)</span><br><span class="line"></span><br><span class="line">   ctx.Abort()</span><br><span class="line">   <span class="keyword">return</span></span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 判断是否过期</span></span><br><span class="line">  <span class="keyword">if</span> time.Now().Unix() &gt; claims.ExpiresAt &#123;</span><br><span class="line">   code = res.TokenExpired</span><br><span class="line">  &#125; <span class="keyword">else</span> <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">   code = res.TokenInvalid</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> code == res.TokenExpired &#123;</span><br><span class="line">   res.Ok__(ctx, res.TokenExpired)</span><br><span class="line">  &#125; <span class="keyword">else</span> <span class="keyword">if</span> code != res.OK &#123;</span><br><span class="line"></span><br><span class="line">   res.Unauthorized(ctx, code)</span><br><span class="line"></span><br><span class="line">   ctx.Abort()</span><br><span class="line">   <span class="keyword">return</span></span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 我在这里的操作是将token对应的user放在了上下文内，</span></span><br><span class="line">  <span class="comment">// 对于需要获取用户的方法，只通过这里就可以拿到userid</span></span><br><span class="line">  <span class="comment">// 而不再需要通过传输参数的方式传入用户信息</span></span><br><span class="line">  ctx.Set(<span class="string">&quot;userid&quot;</span>, claims.Userid)</span><br><span class="line">  ctx.Set(<span class="string">&quot;username&quot;</span>, claims.Username)</span><br><span class="line">  ctx.Next()</span><br><span class="line"> &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="gateway-routes-ping-go"><a href="#gateway-routes-ping-go" class="headerlink" title="gateway/routes/ping.go"></a>gateway/routes/ping.go</h4><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> routes</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line"> <span class="string">&quot;gateway/middleware&quot;</span></span><br><span class="line"> <span class="string">&quot;gateway/proto/pb&quot;</span></span><br><span class="line"> <span class="string">&quot;gateway/utils/res&quot;</span></span><br><span class="line"></span><br><span class="line"> <span class="string">&quot;github.com/gin-gonic/gin&quot;</span></span><br><span class="line"> <span class="string">&quot;github.com/sirupsen/logrus&quot;</span></span><br><span class="line"> <span class="string">&quot;google.golang.org/grpc&quot;</span></span><br><span class="line"> <span class="string">&quot;google.golang.org/grpc/balancer/roundrobin&quot;</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">addPingRoute</span><span class="params">(rg *gin.RouterGroup)</span></span> &#123;</span><br><span class="line"> opts := []grpc.DialOption&#123;</span><br><span class="line">  grpc.WithInsecure(),</span><br><span class="line">  grpc.WithBalancerName(roundrobin.Name),</span><br><span class="line"> &#125;</span><br><span class="line"></span><br><span class="line"> <span class="comment">// 通过etcd做服务发现</span></span><br><span class="line"> pingConn, err := grpc.Dial(<span class="string">&quot;etcd:///ping&quot;</span>, opts...)</span><br><span class="line"> <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">  logrus.Errorf(<span class="string">&quot;try connect ping service failed&quot;</span>)</span><br><span class="line"> &#125;</span><br><span class="line"> <span class="comment">// 不要调用pingConn.Close() 因为addRoute方法只是导入了路由与对应的方法</span></span><br><span class="line"> <span class="comment">// 在读取完毕之后会关闭链接 在读取完毕之后的请求会无法链接 因为链接已经关闭</span></span><br><span class="line"> <span class="comment">// 如果你一定要严谨的关闭 就把它移出去到公共的地方注册链接 并创建方法在优雅关闭内结束链接</span></span><br><span class="line"></span><br><span class="line"> pingClient := pb.NewPingClient(pingConn)</span><br><span class="line"></span><br><span class="line"> ping := rg.Group(<span class="string">&quot;/ping&quot;</span>)</span><br><span class="line"> <span class="comment">// 在这里使用中间件</span></span><br><span class="line"> ping.Use(middleware.JWT())</span><br><span class="line"> &#123;</span><br><span class="line">  ping.GET(<span class="string">&quot;&quot;</span>, <span class="function"><span class="keyword">func</span><span class="params">(ctx *gin.Context)</span></span> &#123;</span><br><span class="line">   name := ctx.Query(<span class="string">&quot;name&quot;</span>)</span><br><span class="line"></span><br><span class="line">   req := &amp;pb.Req&#123;</span><br><span class="line">    Name: name,</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   resp, err := pingClient.Pong(ctx, req)</span><br><span class="line">   <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">    logrus.Errorln(<span class="string">&quot;/v1/ping err, err: &quot;</span>, err)</span><br><span class="line">    res.InternalError(ctx)</span><br><span class="line">    <span class="keyword">return</span></span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   res.Ok(ctx, res.OK, resp.Msg)</span><br><span class="line">  &#125;)</span><br><span class="line"> &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>然后再去请求你就可以实现jwt的鉴权服务了，在需要的路由添加鉴权中间件即可。</p>
<p>如果有同学还不知道jwt的token是怎么生成的，就在 “utils/jwt.go” 中，注释写的很全。流程是用户登录之后，你将用户信息生成token返回给用户，之后用户每次请求都将token存在请求头中的Authorization中，就可以实现鉴权了，别忘了在token过期之后通知用户重新获取token~。</p>
<h2 id="end"><a href="#end" class="headerlink" title="end"></a>end</h2><p>最后的最后，如果有疑问、觉得我哪里没有讲清楚、自己跑不起来或者报错等等等等，可以留言或者email我。我写how to的唯一原因，就是想干掉所有写的不清不楚、看了等于没看的how to。</p>
]]></content>
      <categories>
        <category>微服务</category>
      </categories>
      <tags>
        <tag>etcd</tag>
        <tag>gin</tag>
        <tag>go-grpc</tag>
        <tag>微服务</tag>
      </tags>
  </entry>
  <entry>
    <title>etcd clientv3 全api文档 之 KV</title>
    <url>/2021/12/06/etcd-clientv3-api-document/</url>
    <content><![CDATA[<p>无论是新使用etcd还是需要更深一点了解etcd的人可能都会遇到我曾经遇到的问题：为什么文档这么少？为什么官方文档都不全？这个api怎么用，是什么？网上的教程简略到只有基础方法，各个网页还都是千篇一律的复制粘贴，让当初在使用etcd的我很痛苦。正好在我想要开源一个etcd的web panel的时候，我决定写出一篇文档，从源码下手，对每个api的用法讲解出来。不仅有助于我开源项目的理解，也可以帮助到对etcd不熟悉的小伙伴们。在学习etcd之前，有一个有意思的点可以帮助你理解etcd，就是将所有的key看成水平分布的key，而不是树形结构的key，如：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/a</span><br><span class="line">    /b</span><br><span class="line">        /d</span><br><span class="line">        /e</span><br><span class="line">    /c</span><br><span class="line">        /f</span><br><span class="line">看做</span><br><span class="line">/a</span><br><span class="line">/a/b</span><br><span class="line">/a/b/d</span><br><span class="line">/a/b/e</span><br><span class="line">/a/c</span><br><span class="line">/a/c/f</span><br></pre></td></tr></table></figure>

<p>这样在使用如withPrefix方法时可以更清楚的知道你到底查找、删除了哪一系列的key。</p>
<p>关于clientv3的讲解不涉及具体的etcd源码分析，具体的源码分析会在后面写出。</p>
<h2 id="before-started"><a href="#before-started" class="headerlink" title="before started"></a>before started</h2><p>本文依照的是etcd 版本 3.5.1，etcd的v2与v3区别很大，改变了很多原理，如auth的方式等等；而v3中 3.1 3.2 3.3 3.4 3.5也各不相同，但改动不大，在观看之前需要看清楚版本。具体各个版本的区别再后面会单独写一章。</p>
<h2 id="get-started"><a href="#get-started" class="headerlink" title="get started"></a>get started</h2><p>在etcd全api文档的第一篇，我决定先从最常用的KV下手，包括了存取删除和事务等，基本是新手开始使用etcd最需要知道的几个方法。后面的文章也会以文件为分割来讲述etcd的源码及api使用方法。</p>
<h3 id="KV-struct"><a href="#KV-struct" class="headerlink" title="KV struct"></a>KV struct</h3><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">type</span> kv <span class="keyword">struct</span> &#123;</span><br><span class="line">    remote pb.KVClient</span><br><span class="line">    callOpts []grpc.CallOption</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="第一步，创建一个连接"><a href="#第一步，创建一个连接" class="headerlink" title="第一步，创建一个连接"></a>第一步，创建一个连接</h3><p>首先，你要先创建一个连接，然后才能进行后续的操作</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line">cli, err := clientv3.New(clientv3.Config&#123;</span><br><span class="line">    Endpoints: <span class="string">&quot;example.endpoint&quot;</span>,</span><br><span class="line">    DialTimeout: <span class="number">5</span> * time.Second</span><br><span class="line">&#125;)</span><br><span class="line"><span class="keyword">if</span> err := <span class="literal">nil</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> err</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">defer</span> cli.Close()</span><br></pre></td></tr></table></figure>

<h4 id="注意点"><a href="#注意点" class="headerlink" title="注意点"></a>注意点</h4><ul>
<li>你应该在使用完连接后立即关闭连接，未关闭的连接会使<a href="https://github.com/etcd-io/etcd/tree/main/client/v3#get-started">goroutine泄露</a></li>
<li>虽然使用defer非常方便，也防止你后面忘记关闭连接。但是我更喜欢在操作完方法之后手动调用cli.Close()而不是使用defer，因为每个人的业务场景不一样，方法并不一定会在短时间内结束，可能一次bug导致这个方法一直存活着，你的defer也就一直得不到调用。</li>
</ul>
<h3 id="NewKV"><a href="#NewKV" class="headerlink" title="NewKV"></a>NewKV</h3><p>使用kv与直接使用cli连接调用的区别不大，kv内可以调用的方法与cli一样，具体为调用NewKV会将cli身上的callOpts带到kv上</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">NewKV</span><span class="params">(c *Client)</span> <span class="title">KV</span></span> &#123;</span><br><span class="line">    api := &amp;kv&#123;remote: RetryKVClient(c)&#125;</span><br><span class="line">    <span class="keyword">if</span> c != <span class="literal">nil</span> &#123;</span><br><span class="line">        api.callOpts = c.callOpts</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> api</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="Put"><a href="#Put" class="headerlink" title="Put"></a>Put</h3><p>将一个key-value键值对推入etcd</p>
<p>put方法</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(kv *kv)</span> <span class="title">Put</span><span class="params">(ctx context.Context, key, val <span class="keyword">string</span>, opts ...OpOption)</span> <span class="params">(*PutResponse, error)</span></span></span><br></pre></td></tr></table></figure>

<p>put的调用（使用kv）</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line">kv := clientv3.NewKV(cli)</span><br><span class="line"></span><br><span class="line">resp, err := kv.Put(context.Background(), key, val, ...opts)</span><br></pre></td></tr></table></figure>

<p>put的具体实现</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(kv *kv)</span> <span class="title">Put</span><span class="params">(ctx context.Context, key, val <span class="keyword">string</span>, opts ...OpOption)</span> <span class="params">(*PutResponse, error)</span></span> &#123;</span><br><span class="line"> r, err := kv.Do(ctx, OpPut(key, val, opts...))</span><br><span class="line"> <span class="keyword">return</span> r.put, toErr(ctx, err)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>关于<a href="#Do">Do方法</a></p>
<h3 id="Get"><a href="#Get" class="headerlink" title="Get"></a>Get</h3><p>取回keys，默认会返回你传入key的value，如果传入</p>
<ul>
<li>WithRange(end), get会返回[key, end）范围内的结果</li>
<li>WithFromKey(), get会返回比你传入key大或者相等的key</li>
<li>WithRev(rev) 且 rev &gt; 0，get会返回传入key指定版本的value，如果指定的版本（rev）被压缩（compacted）了，会返回error： ErrCompacted。</li>
<li>WithLimit(limit)，会返回指定数量内的keys</li>
<li>WithSort()， 会对返回的keys进行过滤</li>
</ul>
<p>get方法</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line">Get(ctx context.Context, key <span class="keyword">string</span>, opts ...OpOption) (*GetResponse, error)</span><br></pre></td></tr></table></figure>

<p>get的调用（使用cli）</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line">resp, err := cli.Put(context.Background(), key, ...opts)</span><br></pre></td></tr></table></figure>

<p>get的具体实现</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(kv *kv)</span> <span class="title">Get</span><span class="params">(ctx context.Context, key <span class="keyword">string</span>, opts ...OpOption)</span> <span class="params">(*GetResponse, error)</span></span> &#123;</span><br><span class="line"> r, err := kv.Do(ctx, OpGet(key, opts...))</span><br><span class="line"> <span class="keyword">return</span> r.get, toErr(ctx, err)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>关于<a href="#Do">Do方法</a></p>
<h3 id="Delete"><a href="#Delete" class="headerlink" title="Delete"></a>Delete</h3><p>删除一个key，或者</p>
<ul>
<li>使用WithRange(end)来删除[key, range)范围的key</li>
<li>使用WithPrefix()来删除以key开头的key</li>
</ul>
<p>delete方法</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line">Delete(ctx context.Context, key <span class="keyword">string</span>, opts ...OpOption) (*DeleteResponse, error)</span><br></pre></td></tr></table></figure>

<p>delete的调用方式</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line">resp, err = cli.Delete(ctx, key, ...opts)</span><br></pre></td></tr></table></figure>

<p>delete的具体实现</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line">r, err := kv.Do(ctx, OpDelete(key, opts...))</span><br><span class="line"><span class="keyword">return</span> r.del, toErr(ctx, err)</span><br></pre></td></tr></table></figure>

<p>关于<a href="#Do">Do方法</a></p>
<h3 id="Do"><a href="#Do" class="headerlink" title="Do"></a>Do</h3><p>Do这个方法为你可以将多种/次操作融合到一个方法内，并在之后（重复）调用。具体如何使用除了官方方法外，就要看具体的业务需求。如你可以将put-&gt;get-&gt;delete这一套流程融合到一个Do里。通过源码可以知道，官方的get、put等也是通过Do方法来实现，然后在Do内实现具体的get、put的etcd调用，区分于事务级别的调用。</p>
<p>Do方法</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line">Do(ctx context.Context, op Op) (OpResponse, error)</span><br></pre></td></tr></table></figure>

<p>Do的调用方式</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line">ops := []clientv3.Op&#123;</span><br><span class="line">   clientv3.OpPut(<span class="string">&quot;put-key&quot;</span>, <span class="string">&quot;123&quot;</span>),</span><br><span class="line">   clientv3.OpGet(<span class="string">&quot;put-key&quot;</span>),</span><br><span class="line">   clientv3.OpPut(<span class="string">&quot;put-key&quot;</span>, <span class="string">&quot;456&quot;</span>)&#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">for</span> _, op := <span class="keyword">range</span> ops &#123;</span><br><span class="line">   <span class="keyword">if</span> _, err := cli.Do(context.TODO(), op); err != <span class="literal">nil</span> &#123;</span><br><span class="line">    log.Fatal(err)</span><br><span class="line">   &#125;</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<p>Do的具体实现</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(kv *kv)</span> <span class="title">Do</span><span class="params">(ctx context.Context, op Op)</span> <span class="params">(OpResponse, error)</span></span> &#123;</span><br><span class="line"> <span class="keyword">var</span> err error</span><br><span class="line"> <span class="keyword">switch</span> op.t &#123; <span class="comment">// 对传入的op遍历</span></span><br><span class="line"> <span class="keyword">case</span> tRange:</span><br><span class="line">  <span class="keyword">var</span> resp *pb.RangeResponse</span><br><span class="line">  resp, err = kv.remote.Range(ctx, op.toRangeRequest(), kv.callOpts...)</span><br><span class="line">  <span class="keyword">if</span> err == <span class="literal">nil</span> &#123;</span><br><span class="line">   <span class="keyword">return</span> OpResponse&#123;get: (*GetResponse)(resp)&#125;, <span class="literal">nil</span></span><br><span class="line">  &#125;</span><br><span class="line"> <span class="keyword">case</span> tPut:</span><br><span class="line">  <span class="keyword">var</span> resp *pb.PutResponse</span><br><span class="line">  r := &amp;pb.PutRequest&#123;Key: op.key, Value: op.val, Lease: <span class="keyword">int64</span>(op.leaseID), PrevKv: op.prevKV, IgnoreValue: op.ignoreValue, IgnoreLease: op.ignoreLease&#125;</span><br><span class="line">  resp, err = kv.remote.Put(ctx, r, kv.callOpts...)</span><br><span class="line">  <span class="keyword">if</span> err == <span class="literal">nil</span> &#123;</span><br><span class="line">   <span class="keyword">return</span> OpResponse&#123;put: (*PutResponse)(resp)&#125;, <span class="literal">nil</span></span><br><span class="line">  &#125;</span><br><span class="line"> <span class="keyword">case</span> tDeleteRange:</span><br><span class="line">  <span class="keyword">var</span> resp *pb.DeleteRangeResponse</span><br><span class="line">  r := &amp;pb.DeleteRangeRequest&#123;Key: op.key, RangeEnd: op.end, PrevKv: op.prevKV&#125;</span><br><span class="line">  resp, err = kv.remote.DeleteRange(ctx, r, kv.callOpts...)</span><br><span class="line">  <span class="keyword">if</span> err == <span class="literal">nil</span> &#123;</span><br><span class="line">   <span class="keyword">return</span> OpResponse&#123;del: (*DeleteResponse)(resp)&#125;, <span class="literal">nil</span></span><br><span class="line">  &#125;</span><br><span class="line"> <span class="keyword">case</span> tTxn:</span><br><span class="line">  <span class="keyword">var</span> resp *pb.TxnResponse</span><br><span class="line">  resp, err = kv.remote.Txn(ctx, op.toTxnRequest(), kv.callOpts...)</span><br><span class="line">  <span class="keyword">if</span> err == <span class="literal">nil</span> &#123;</span><br><span class="line">   <span class="keyword">return</span> OpResponse&#123;txn: (*TxnResponse)(resp)&#125;, <span class="literal">nil</span></span><br><span class="line">  &#125;</span><br><span class="line"> <span class="keyword">default</span>:</span><br><span class="line">  <span class="built_in">panic</span>(<span class="string">&quot;Unknown op&quot;</span>)</span><br><span class="line"> &#125;</span><br><span class="line"> <span class="keyword">return</span> OpResponse&#123;&#125;, toErr(ctx, err)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="Txn"><a href="#Txn" class="headerlink" title="Txn"></a>Txn</h3><p>txn用来创建一个事务，将后续操作以事务进行, 事务有四种子方法</p>
<ul>
<li>If</li>
<li>Then</li>
<li>Else</li>
<li>Commit</li>
</ul>
<p>Txn方法</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line">Txn(ctx context.Context) Txn</span><br></pre></td></tr></table></figure>

<p>txn的调用例子</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line">kvc := clientv3.NewKV(cli)</span><br><span class="line"></span><br><span class="line">  _, err = kvc.Put(context.TODO(), <span class="string">&quot;key&quot;</span>, <span class="string">&quot;xyz&quot;</span>)</span><br><span class="line">  <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">   log.Fatal(err)</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  ctx, cancel := context.WithTimeout(context.Background(), requestTimeout)</span><br><span class="line">  _, err = kvc.Txn(ctx).</span><br><span class="line">   <span class="comment">// 事务的值是字典类型的比较</span></span><br><span class="line">   If(clientv3.Compare(clientv3.Value(<span class="string">&quot;key&quot;</span>), <span class="string">&quot;&gt;&quot;</span>, <span class="string">&quot;abc&quot;</span>)).</span><br><span class="line">   <span class="comment">// &quot;xyz&quot; &gt; &quot;abc&quot; 时运行</span></span><br><span class="line">   Then(clientv3.OpPut(<span class="string">&quot;key&quot;</span>, <span class="string">&quot;XYZ&quot;</span>)).</span><br><span class="line">   <span class="comment">// else运行</span></span><br><span class="line">   Else(clientv3.OpPut(<span class="string">&quot;key&quot;</span>, <span class="string">&quot;ABC&quot;</span>)).</span><br><span class="line">   Commit()</span><br></pre></td></tr></table></figure>

<p>txn的源码</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(kv *kv)</span> <span class="title">Txn</span><span class="params">(ctx context.Context)</span> <span class="title">Txn</span></span> &#123;</span><br><span class="line"> <span class="keyword">return</span> &amp;txn&#123;</span><br><span class="line">  kv:       kv,</span><br><span class="line">  ctx:      ctx,</span><br><span class="line">  callOpts: kv.callOpts,</span><br><span class="line"> &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="Compact"><a href="#Compact" class="headerlink" title="Compact"></a>Compact</h3><p>compact，压缩key-value，为了防止历史记录越来越大。传入指定的rev，会压缩rev之后的key-value历史</p>
<p>compact方法</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line">Compact(ctx context.Context, rev <span class="keyword">int64</span>, opts ...CompactOption) (*CompactResponse, error)</span><br></pre></td></tr></table></figure>

<p>compact的调用方式</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line">resp, err := cli.Get(ctx, <span class="string">&quot;foo&quot;</span>)</span><br><span class="line">  cancel()</span><br><span class="line">  <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">   log.Fatal(err)</span><br><span class="line">  &#125;</span><br><span class="line">  compRev := resp.Header.Revision <span class="comment">// 具体你要压缩哪个版本之前的key-val</span></span><br><span class="line"></span><br><span class="line">  ctx, cancel = context.WithTimeout(context.Background(), requestTimeout)</span><br><span class="line">  _, err = cli.Compact(ctx, compRev)</span><br></pre></td></tr></table></figure>

<p>compact的具体实现</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(kv *kv)</span> <span class="title">Compact</span><span class="params">(ctx context.Context, rev <span class="keyword">int64</span>, opts ...CompactOption)</span> <span class="params">(*CompactResponse, error)</span></span> &#123;</span><br><span class="line"> resp, err := kv.remote.Compact(ctx, OpCompact(rev, opts...).toRequest(), kv.callOpts...)</span><br><span class="line"> <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="literal">nil</span>, toErr(ctx, err)</span><br><span class="line"> &#125;</span><br><span class="line"> <span class="keyword">return</span> (*CompactResponse)(resp), err</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="end"><a href="#end" class="headerlink" title="end"></a>end</h2><p>这是关于clientv3 api讲解的第一章，如果你有什么建议、想法、编写错误的指出或者好的想法，欢迎通过email或issue的方式来告诉我。</p>
]]></content>
      <tags>
        <tag>etcd</tag>
        <tag>clientv3</tag>
      </tags>
  </entry>
  <entry>
    <title>使用docker部署一套相比ELK更轻量的日志系统LPG</title>
    <url>/2021/11/16/how-to-lpg/</url>
    <content><![CDATA[<h2 id="Why-this"><a href="#Why-this" class="headerlink" title="Why this"></a>Why this</h2><p>使用过ELK日志系统的话，你可能知道Logstash强大，它可以实现数据传输、格式处理、格式化输出，和强大的插件功能，经常用于日志处理，但是同时logstash也较为消耗资源，占用了很多的CPU和内存，并且由于没有消息队列缓存，存在数据丢失的隐患。很多时候我们对日志的需求并没有那么高，并不需要盯着日志分析结果，只是简单的看几个参数，有没有报错就停止了。更加常见的场景是，我们的服务器并没有那么多余的空间留给日志系统。</p>
<p>所以这篇文章来介绍一套轻量的日志系统LPG（loki、promtail、grafana）</p>
<ul>
<li>loki: 主服务器，负责存储日志和处理查询</li>
<li>promtail: 代理，负责收集日志发送给loki</li>
<li>grafana: 看板，用来查询和显示日志</li>
</ul>
<p>loki: like Prometheus, but for logs(像普罗米修斯监控一样，只不过是监控日志)，受到普罗米修斯的启发的水平拓展、高可用、多租户日志聚合系统。</p>
<p>和其他聚合日志系统相比，loki：</p>
<ul>
<li>不对日志进行全文索引。通过存储压缩的非结构化日志和仅索引元数据，loki更易于操作且运行成本更低。</li>
<li>使用您已经在 Prometheus 中使用的相同标签对日志流进行索引和分组，使您能够使用您已经在 Prometheus 中使用的相同标签在指标和日志之间无缝切换。</li>
<li>尤其适合存储Kubernetes Pod 日志。Pod 标签等元数据会被自动抓取和索引。</li>
<li>在 Grafana 看板中有图形界面支持（需要 Grafana v6.0）</li>
</ul>
<p>根据官方介绍，可以了解到loki的优点是不会对日志全文索引，仅索引“标签”；压缩了非结构化的日志让体积更小；接下来介绍如何使用docker部署LPG日志系统</p>
<h2 id="Get-Started"><a href="#Get-Started" class="headerlink" title="Get Started"></a>Get Started</h2><h3 id="loki-yml"><a href="#loki-yml" class="headerlink" title="loki.yml"></a>loki.yml</h3><p>这个文件是配置loki相关的，在docker-compose文件中会映射入loki的image内，我会把可能你会涉及到的配置标注上注释，更具体的配置请参考<a href="https://grafana.com/docs/loki/latest/configuration/">loki 官方文档</a></p>
<figure class="highlight yml"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 认证相关</span></span><br><span class="line"><span class="attr">auth_enabled:</span> <span class="literal">false</span></span><br><span class="line"></span><br><span class="line"><span class="attr">server:</span></span><br><span class="line">    <span class="comment"># 监听的端口 因为promtail是主动推送日志给loki 所以需要监听端口号</span></span><br><span class="line">    <span class="attr">http_listen_port:</span> <span class="number">3100</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 配置 ingester 以及 ingester 如何将自己注册到键值存储。</span></span><br><span class="line"><span class="attr">ingester:</span></span><br><span class="line">    <span class="attr">lifecycler:</span></span><br><span class="line">        <span class="comment"># ip地址</span></span><br><span class="line">        <span class="attr">address:</span> <span class="number">127.0</span><span class="number">.0</span><span class="number">.1</span></span><br><span class="line">        <span class="attr">ring:</span></span><br><span class="line">            <span class="attr">kvstore:</span></span><br><span class="line">                <span class="comment"># 存在内存中</span></span><br><span class="line">                <span class="attr">store:</span> <span class="string">inmemory</span></span><br><span class="line">            <span class="attr">replication_factor:</span> <span class="number">1</span></span><br><span class="line">        <span class="attr">final_sleep:</span> <span class="string">0s</span></span><br><span class="line">    <span class="comment"># 指定时间内的块（chunk）未接收到新日志将会被清空</span></span><br><span class="line">    <span class="attr">chunk_idle_period:</span> <span class="string">1h</span></span><br><span class="line">    <span class="comment"># 所有的chunk都将在指定时间后被清空，默认1h</span></span><br><span class="line">    <span class="attr">max_chunk_age:</span> <span class="string">1h</span></span><br><span class="line">    <span class="comment"># 每个chunk大小为1.5M</span></span><br><span class="line">    <span class="attr">chunk_target_size:</span> <span class="number">1048576</span></span><br><span class="line">    <span class="comment"># 如果使用索引缓存，则这个时间必须大于索引读取缓存的TTL（默认索引读取缓存 TTL 为 5m）</span></span><br><span class="line">    <span class="attr">chunk_retain_period:</span> <span class="string">30s</span></span><br><span class="line">    <span class="comment"># 最大尝试次数 0为disabled</span></span><br><span class="line">    <span class="attr">max_transfer_retries:</span> <span class="number">0</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 配置块索引模式及其存储位置</span></span><br><span class="line"><span class="attr">schema_config:</span></span><br><span class="line">    <span class="attr">configs:</span></span><br><span class="line">        <span class="bullet">-</span> <span class="attr">from:</span> <span class="number">2019-11-17</span> <span class="comment"># 创建索引存储区的第一天的日期。如果这是您唯一的 period_config，请使用过去的日期. 否则请使用您希望架构切换的日期。 YYYY-MM-DD 格式，例如：2018-04-15。</span></span><br><span class="line">          <span class="attr">store:</span> <span class="string">boltdb-shipper</span> <span class="comment"># 用于索引的储存</span></span><br><span class="line">          <span class="attr">object_store:</span> <span class="string">filesystem</span></span><br><span class="line">          <span class="attr">schema:</span> <span class="string">v11</span></span><br><span class="line">          <span class="attr">index:</span></span><br><span class="line">            <span class="attr">prefix:</span> <span class="string">index_</span></span><br><span class="line">            <span class="attr">period:</span> <span class="string">24h</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 配置索引和块的存储</span></span><br><span class="line"><span class="attr">storage_config:</span></span><br><span class="line">    <span class="attr">boltdb_shipper:</span></span><br><span class="line">        <span class="attr">active_index_directory:</span> <span class="string">/loki/boltdb-shipper-active</span></span><br><span class="line">        <span class="attr">cache_location:</span> <span class="string">/loki/boltdb-shipper-cache</span></span><br><span class="line">        <span class="attr">cache_ttl:</span> <span class="string">24h</span> <span class="comment"># 缓存保存时长，可以提升长查询的速度，但是会占据更多的空间</span></span><br><span class="line">        <span class="attr">shared_store:</span> <span class="string">filesystem</span></span><br><span class="line">    <span class="attr">filesystem:</span></span><br><span class="line">        <span class="attr">directory:</span> <span class="string">/loki/chunks</span></span><br><span class="line"></span><br><span class="line"><span class="attr">compactor:</span></span><br><span class="line">    <span class="attr">working_directory:</span> <span class="string">/loki/boltdb-shipper-compactor</span></span><br><span class="line">    <span class="attr">shared_store:</span> <span class="string">filesystem</span></span><br><span class="line"></span><br><span class="line"><span class="attr">limits_config:</span></span><br><span class="line">    <span class="attr">reject_old_samples:</span> <span class="literal">true</span></span><br><span class="line">    <span class="attr">reject_old_samples_max_age:</span> <span class="string">168h</span></span><br><span class="line"></span><br><span class="line"><span class="attr">chunk_store_config:</span></span><br><span class="line">    <span class="attr">max_look_back_period:</span> <span class="string">0s</span></span><br><span class="line"></span><br><span class="line"><span class="attr">table_manager:</span></span><br><span class="line">    <span class="attr">retention_deletes_enabled:</span> <span class="literal">false</span></span><br><span class="line">    <span class="attr">retention_period:</span> <span class="string">0s</span></span><br><span class="line"></span><br><span class="line"><span class="attr">ruler:</span></span><br><span class="line">    <span class="attr">storage:</span></span><br><span class="line">        <span class="attr">type:</span> <span class="string">local</span></span><br><span class="line">        <span class="attr">local:</span></span><br><span class="line">            <span class="attr">directory:</span> <span class="string">/loki/rules</span></span><br><span class="line">    <span class="attr">rule_path:</span> <span class="string">/loki/rules-temp</span></span><br><span class="line">    <span class="attr">alertmanager_url:</span> <span class="string">http://localhost:9093</span></span><br><span class="line">    <span class="attr">ring:</span></span><br><span class="line">        <span class="attr">kvstore:</span></span><br><span class="line">            <span class="attr">store:</span> <span class="string">inmemory</span></span><br><span class="line">    <span class="attr">enable_api:</span> <span class="literal">true</span></span><br></pre></td></tr></table></figure>

<h3 id="promtail-yml"><a href="#promtail-yml" class="headerlink" title="promtail.yml"></a>promtail.yml</h3><p>这个文件是配置promtail相关的，注释情况如上，更具体的配置请参考<a href="https://grafana.com/docs/loki/latest/clients/promtail/configuration/">promtail 官方文档</a></p>
<figure class="highlight yml"><table><tr><td class="code"><pre><span class="line"><span class="attr">server:</span></span><br><span class="line">    <span class="attr">http_listen_port:</span> <span class="number">9080</span> <span class="comment"># 监听端口</span></span><br><span class="line">    <span class="attr">grpc_listen_port:</span> <span class="number">0</span> <span class="comment"># grpc 监听端口</span></span><br><span class="line"></span><br><span class="line"><span class="attr">positions:</span></span><br><span class="line">  <span class="attr">filename:</span> <span class="string">/tmp/positions.yaml</span></span><br><span class="line"></span><br><span class="line"><span class="attr">clients:</span></span><br><span class="line">  <span class="bullet">-</span> <span class="attr">url:</span> <span class="string">http://loki:3100/loki/api/v1/push</span> <span class="comment"># loki的通讯地址 与 loki.yml内的监听端口对应</span></span><br><span class="line"></span><br><span class="line"><span class="attr">scrape_configs:</span></span><br><span class="line">  <span class="bullet">-</span> <span class="attr">job_name:</span> <span class="string">system</span></span><br><span class="line">    <span class="attr">static_configs:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="attr">targets:</span></span><br><span class="line">          <span class="bullet">-</span> <span class="string">localhost</span></span><br><span class="line">        <span class="attr">labels:</span></span><br><span class="line">          <span class="attr">job:</span> <span class="string">varlogs</span></span><br><span class="line">          <span class="attr">__path__:</span> <span class="string">/var/log/*log</span> <span class="comment"># 在此处配置你要监听的日志的地址所存储的地址</span></span><br><span class="line"></span><br></pre></td></tr></table></figure>

<h3 id="docker-compose-yml"><a href="#docker-compose-yml" class="headerlink" title="docker-compose.yml"></a>docker-compose.yml</h3><p>最后是lpg的compose文件, 具体每一行做了什么我会将注释写在文件里，如果你完全看不懂docker-compose，可以先尝试学习docker。</p>
<figure class="highlight dockerfile"><table><tr><td class="code"><pre><span class="line">version: <span class="string">&#x27;3&#x27;</span></span><br><span class="line"></span><br><span class="line">services:</span><br><span class="line">    <span class="comment"># 日志的存储和解析</span></span><br><span class="line">    loki:</span><br><span class="line">        image: grafana/loki</span><br><span class="line">        container_name: lpg-promtail</span><br><span class="line">        volumes:</span><br><span class="line">            <span class="comment"># 持久化loki配置 映射的地址可以根据自己的需求更改</span></span><br><span class="line">            - ../data/lpg/loki:/etcd/loki</span><br><span class="line">            <span class="comment"># loki的配置文件</span></span><br><span class="line">            - ./loki.yml:/etc/loki-conf/loki.yml</span><br><span class="line">        command: -config.file=/etcd/loki-conf/loki.yml</span><br><span class="line">        ports:</span><br><span class="line">            - <span class="number">3100</span>:<span class="number">3100</span></span><br><span class="line">        networks:</span><br><span class="line">            - lpg</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 日志收集器</span></span><br><span class="line">    promtail:</span><br><span class="line">        image: grafana/promtail</span><br><span class="line">        container_name: lpg-promtail</span><br><span class="line">        volumes:</span><br><span class="line">            <span class="comment"># 你需要将要监听的日志映射入容器内 使用编写软连接等方式将日志收集到一个文件夹内</span></span><br><span class="line">            <span class="comment"># 通过promtail.yml对应的检索方式发现日志文件，修改左边的地址到你对应的日志存储地址</span></span><br><span class="line">            - ../data/lpg/logs:/var/log</span><br><span class="line">            <span class="comment"># 持久化promtail</span></span><br><span class="line">            - ../data/lpg/promtail:/etc/promtail</span><br><span class="line">            <span class="comment"># promtail的配置文件</span></span><br><span class="line">            - ./promtail.yml:/etc/promtail-conf/promtail.yml</span><br><span class="line">        command: -config.file=/etc/promtail-conf/promtail.yml</span><br><span class="line">        networks:</span><br><span class="line">            - lpg</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 日志看板 可视化</span></span><br><span class="line">    grafana:</span><br><span class="line">        image: grafana/grafana</span><br><span class="line">        container_name: lpg-grafana</span><br><span class="line">        ports:</span><br><span class="line">            - <span class="number">3000</span>:<span class="number">3000</span></span><br><span class="line">        networks:</span><br><span class="line">            - lpg</span><br><span class="line"></span><br><span class="line">networks:</span><br><span class="line">    lpg:</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<h3 id="文件结构"><a href="#文件结构" class="headerlink" title="文件结构"></a>文件结构</h3><ul>
<li>compose<ul>
<li>data<ul>
<li>lpg<ul>
<li>logs 日志集合</li>
<li>loki loki持久化</li>
<li>promtail promtail持久化</li>
</ul>
</li>
</ul>
</li>
<li>lpg<ul>
<li>loki.yml loki配置文件</li>
<li>docker-compose.yml compose文件</li>
<li>promtail.yml # promtail配置文件</li>
</ul>
</li>
</ul>
</li>
</ul>
<p>这个结构只是一个参考，如果你会用docker，可以自己配置文件的位置和存储的地址等</p>
<h3 id="启动compose文件"><a href="#启动compose文件" class="headerlink" title="启动compose文件"></a>启动compose文件</h3><p>在编写完docker-compose文件之后，就可以启动了。需要注意的是，在启动promtail之前，promtail抓取日志的路径下应该至少存在一个符合抓取规则的日志，如果没有找到的话，promtail就会停止抓取。很多人一开始在grafana看板中找不到日志也是这个原因。</p>
<h2 id="配置grafana"><a href="#配置grafana" class="headerlink" title="配置grafana"></a>配置grafana</h2><p>启动了compose文件之后，就可以进入grafana看板的地址查看效果了，启动的地址在3000（或者你自己设置的）端口，默认的账户名和密码都是admin。</p>
<p>在登录进入grafana之后，首先你应该配置数据来源(data source)，如图1、2、3所示：</p>
<p><img src="/images/lpg_grafana_datasource_1.png" alt="配置数据来源1"><br><img src="/images/lpg_grafana_datasource_2.png" alt="配置数据来源2"><br><img src="/images/lpg_grafana_datasource_3.png" alt="配置数据来源3"></p>
<p>在配置完数据来源后，进入Explore查看当前的抓取情况，配置如图4、5、6所示：</p>
<p><img src="/images/lpg_grafana_explore_1.png" alt="配置查看页面4"></p>
<p>根据图4的Add query会发现，一个grafana是可以对应多个loki的，对于有分布式部署日志收集需求的，就可以在这里设置多个数据来源来查看</p>
<p><img src="/images/lpg_grafana_explore_2.png" alt="配置查看页面5"></p>
<p>点击展开图5的步骤2展开列表，选择你想通过哪种label来查看日志，我这里是通过文件名来筛选，然后在步骤4选择具体的文件。步骤5、6分别是查询的类型和最多限制多少行日志分页。 并且你还可以自己编写resulting selector（点击步骤8可以看到具体的语法详情）</p>
<p><img src="/images/lpg_grafana_explore_3.png" alt="配置查看页面6"></p>
<p>在这里就基本大功告成了，你可以看到日志在时间内的产生速率，和各种过滤日志的方法，比如服务的日志存在大量重复的输出，就可以在Dedup(去重)处选择Signature，以特征过滤重复日志。根据图5中的步骤6来设置的每段多少行内容进行的分段等等。在这里还差一点，那就是我想看某一个时间段的日志，或者项目新版本刚上线，需要实时观看日志的产生可不可以呢？当然可以</p>
<p><img src="/image/lpg_grafana_livestream.png" alt="live stream"></p>
<p>在grafana看板右上角有按照你的需求查询和实时查询两个选项。可以通过这两个按钮查找所需时段的日志和实时产生的日志。在这里基本就是lpg的全部功能了。</p>
<h2 id="end"><a href="#end" class="headerlink" title="end"></a>end</h2><p>到这里lpg部署的全部工作就完成了，如果你看到了这里，恭喜你学会了一套轻量、横向扩展的日志系统。如果在部署的过程中遇到了跟本文流程冲突的bug或者问题。可以在评论区留言或者通过邮箱联系我，我会尽量解答你的问题~</p>
]]></content>
      <categories>
        <category>系统</category>
      </categories>
      <tags>
        <tag>loki</tag>
        <tag>日志系统</tag>
      </tags>
  </entry>
</search>
